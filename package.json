{
  "name": "saybuild",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "dev:web": "pnpm --filter web dev",
    "dev:reader": "pnpm --filter reader dev",
    "dev:writer": "pnpm --filter writer dev",
    "init-db": "tsx scripts/init-db.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
