{
  "name": "saybuild",
  "private": true,
  "scripts": {
    "dev": "pnpm build:mcp && pnpm --filter web dev",
    "build:mcp": "pnpm --filter component-tree-services build",
    "build": "pnpm build:mcp && pnpm --filter web build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
